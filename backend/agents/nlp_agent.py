class NLPAgent:
    def extract_entities(self, notes: str):
        return {
            "suspected_diagnoses": ["Fever (mocked)"],
            "symptoms": ["Headache (mocked)"],
            "requested_procedures": ["General Checkup (mocked)"]
        }
